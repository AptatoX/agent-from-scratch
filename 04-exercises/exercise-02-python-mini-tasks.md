# Exercise 02: Python Mini Tasks

## 题目 1

写一个函数，返回一段固定文本：

```python
def say_agent():
    return "I am learning Agent."
```

## 题目 2

写一个函数，根据输入决定返回哪个工具名：

```python
def choose_tool(text):
    if "time" in text:
        return "get_time"
```

## 题目 3

想一想：为什么 `save_note(text)` 需要参数，而 `get_time()` 通常不需要？

## 学习目标

做完这几个小题后，你应该更熟悉：

- 函数
- 返回值
- 判断逻辑
- 参数的作用
