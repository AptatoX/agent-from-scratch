# Exercise 01: Understanding Agent

试着用自己的话回答下面几个问题。

## 题目

1. 普通 chat 和 `Agent` 的核心差别是什么？
2. 为什么 `save_note` 属于 action，而不只是 reply？
3. 为什么一个最小 `Agent` 里需要 `observation`？

## 参考方向

你不需要写成标准答案。

只要能表达出这些意思，就说明你已经在理解：

- `Agent` 不只是生成文本
- `Agent` 会决定并执行动作
- 工具结果需要被读取，再组织成回复
